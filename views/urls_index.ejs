<!DOCTYPE html>

<html lang="en">
<head>
  <title>TinyApp</title>
</head>

<body>
    <!-- Add your EJS code here -->
    <ul>
      <% for(var i in templateVars){ %>
        <li><%= templateVars[i] %></li>
      <% } %>
    </ul>
</body>
</html>