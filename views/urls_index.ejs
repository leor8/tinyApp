<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TinyApp</title>
  </head>

  <body>
    <!-- Add your EJS code here -->
    <% include partials/_header %>

    <h2> Database </h2>
      <% for(var i in urls){ %>
        <form method="GET" action="<%= urls[i].url %>">
          <p><%= i %> --> <a href="<%= urls[i].url %>"><%= urls[i].url %></a></p>
          <a href="urls/<%= i %>">Edit</a>
          <button type="submit">Delete</button>
        </form>
      <% } %>
  </body>
</html>